# Copy to .env and fill in. This file holds SECRETS — never commit it.
# Use a bucket-scoped R2 API token with Object Read & Write (not your account keys).
R2_ACCESS_KEY=
R2_SECRET_KEY=

# If backend: s3 instead of r2, use these instead:
# S3_ACCESS_KEY=
# S3_SECRET_KEY=
